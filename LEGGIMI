il file "crea_eseguibile.txt" contiene il codice che ho utilizzato per creare la cartella con l'eseguibile.
Utilizza il modulo pypi chiamato "pyinstaller".
Dove lo eseguite crea una cartella "build" (contiene la compilazione del codice, può essere cancellata alla fine) 
e una cartella "dist" (che contiene la cartella con l'eseguibile creato).

Il file "InstallerSetupScript.iss" è stato creato con InnoSetup (https://jrsoftware.org/isinfo.php). Contiene le 
istruzioni per creare l'installer per Windows, data la cartella "dist" creata prima.

Questo conclude il corso di Informatica del Quarto Anno ;)

